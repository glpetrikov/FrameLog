name: CI
on: [push, pull_request]

jobs:
  build-fedora:
    runs-on: ubuntu-latest
    container:
      image: fedora:latest

    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: dnf install -y gcc-c++ make wget tar

      - name: Install premake
        run: |
          wget https://github.com/premake/premake-core/releases/download/v5.0.0-beta2/premake-5.0.0-beta2-linux.tar.gz
          tar -xzf premake-5.0.0-beta2-linux.tar.gz
          mv premake5 /usr/local/bin/premake5

      - name: Generate and build all projects
        run: |
          for dir in benchmarks source tests examples; do
            if [ -f "$dir/premake5.lua" ]; then
              echo "=== Building $dir ==="
              cd "$dir"
              premake5 gmake2
              cd build
              make config=release
              make config=debug
              cd ../..
            fi
          done
